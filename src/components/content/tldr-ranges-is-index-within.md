Tells, is a given natural number index within any of the {% if page.url !== "/ranges/" %}<a href="/ranges/">{% endif %}_ranges_{% if page.url !== "/ranges/" %}</a>{% endif %}. It's a wrapper on top of `Array.prototype.find()`.
